# ===========================================
# Loktis - Configuration (loktis.pl)
# ===========================================
# This file contains all environment variables for both frontend and backend.
# Copy this file to .env and fill in your values.

# -------------------------------------------
# BACKEND (Django)
# -------------------------------------------

# Gemini AI Configuration (for floor plan analysis)
# Get your API key from: https://aistudio.google.com
GEMINI_API_KEY=your_gemini_api_key_here

# Django Secret Key (generate a new one for production)
# python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
DJANGO_SECRET_KEY=django-insecure-change-me-in-production

# Debug mode (set to False in production)
DEBUG=True

# Allowed hosts (comma-separated, e.g., your-app.up.railway.app,localhost)
ALLOWED_HOSTS=localhost,127.0.0.1

# CORS origins (comma-separated frontend URLs)
CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

# Database URL (PostgreSQL for production)
# Railway provides this automatically as DATABASE_URL
# DATABASE_URL=postgresql://user:password@host:5432/dbname

# -------------------------------------------
# FRONTEND (Vite/Vue)
# -------------------------------------------

# Google Maps API Key (for map display)
# Get your API key from: https://console.cloud.google.com/google/maps-apis
# Enable "Maps JavaScript API" in your project
VITE_GOOGLE_MAPS_API_KEY=your_google_maps_api_key_here

# Backend API URL (change to Railway URL in production)
VITE_API_URL=http://localhost:8000/api

# -------------------------------------------
# LOKTIS — Konfiguracja analizy lokalizacji
# -------------------------------------------

# Overpass API
# OVERPASS_MODE może być 'public' (domyślnie oparty na OVERPASS_URL) lub 'local' (oparty na OVERPASS_LOCAL_URL)
OVERPASS_MODE=public
OVERPASS_LOCAL_URL=http://localhost:12345/api/interpreter
OVERPASS_URL=https://overpass-api.de/api/interpreter
OVERPASS_FALLBACK_URLS=https://lz4.overpass-api.de/api/interpreter,https://z.overpass-api.de/api/interpreter
OVERPASS_TIMEOUT=60

# Google Places API
GOOGLE_PLACES_ENABLED=true
GOOGLE_PLACES_API_KEY=
GOOGLE_MAX_RETRIES=2

# Domyślne flagi analizy
DEFAULT_ENRICHMENT=false
DEFAULT_FALLBACK=true
DEFAULT_POI_PROVIDER=hybrid
DEFAULT_RADIUS=500

# Sekcje raportu (włącz/wyłącz)
REPORT_AIR_QUALITY=true
REPORT_NOISE_ANALYSIS=false
REPORT_AI_INSIGHTS=true
REPORT_NATURE_METRICS=true
REPORT_DATA_QUALITY=true

# Jakość powietrza
AIR_QUALITY_PROVIDER=open_meteo
AIR_QUALITY_ENABLED=true

# Rate Limiting
RATE_LIMIT_PER_MINUTE=5
RATE_LIMIT_PER_HOUR=30

# Cache TTLs (sekundy)
CACHE_TTL_LISTING=3600
CACHE_TTL_POIS=604800
CACHE_TTL_GOOGLE_DETAILS=604800
CACHE_TTL_GOOGLE_NEARBY=259200

